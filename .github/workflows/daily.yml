name: Daily PSX analysis
on:
  schedule:
    # 5:00 PM PKT = 12:00 UTC, Monday-Friday
    - cron: "0 12 * * 1-5"
  workflow_dispatch: {}   # manual run button

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pandas numpy psxdata
      - run: python daily_update.py
      - name: Commit results
        run: |
          git config user.name "psx-bot"
          git config user.email "bot@users.noreply.github.com"
          git add data/psx.sqlite output/
          git commit -m "Daily analysis $(date -u +%F)" || echo "no changes"
          git push
